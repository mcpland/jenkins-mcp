# Changesets

Use Changesets to manage versioning and releases.

- `pnpm changeset` creates a new changeset entry.
- `pnpm version-packages` updates package versions and changelogs.
- `pnpm release` publishes packages to npm.
